<UserControl x:Class="CloudsdaleWin7.Controls.MessageTextControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <RichTextBox x:Name="RichText" BorderBrush="{x:Null}" IsTabStop="False" IsReadOnly="True" IsHitTestVisible="True" IsDocumentEnabled="True" AllowDrop="False" AcceptsReturn="False" BorderThickness="0" >
            <RichTextBox.Background>
                <SolidColorBrush Color="{DynamicResource PrimaryBackground}"/>
            </RichTextBox.Background>
            <RichTextBox.Style>
                <Style>
                </Style>
            </RichTextBox.Style>
        </RichTextBox>
    </Grid>
</UserControl>

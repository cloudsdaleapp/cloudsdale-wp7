<phone:PhoneApplicationPage 
    x:Class="Cloudsdale.Clouds"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Generic="clr-namespace:System.Collections.Generic;assembly=System"
    xmlns:System="clr-namespace:System;assembly=mscorlib" xmlns:Cloudsdale="clr-namespace:Cloudsdale" mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="768"
    FontFamily="Segoe WP Bold"
    FontSize="28"
    Foreground="Black"
    SupportedOrientations="PortraitOrLandscape"  Orientation="Portrait"
    shell:SystemTray.IsVisible="True" OrientationChanged="PhoneApplicationPageOrientationChanged">

    <phone:PhoneApplicationPage.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="XamlResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </phone:PhoneApplicationPage.Resources>

    <Grid x:Name="LayoutRoot" Background="{StaticResource PortraitBackground}">
        <controls:Pivot Title="&lt;Cloud name goes here&gt;" x:Name="cloudPivot">
            <controls:Pivot.HeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" FontFamily="Segoe WP Bold" FontSize="48"/>
                </DataTemplate>
            </controls:Pivot.HeaderTemplate>
            <controls:PivotItem x:Name="chatTab" Header="Chat">
                <Grid Margin="0,-30,0,0">
                    <Grid VerticalAlignment="Bottom" Height="100" Margin="-12,0">
                        <Grid.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#FFF0F0F0" Offset="0.0" />
                                <GradientStop Color="#FFC7C7C7" Offset="0.4" />
                                <GradientStop Color="#FFC7C7C7" Offset="1" />
                            </LinearGradientBrush>
                        </Grid.Background>
                        <TextBox VerticalAlignment="Bottom" x:Name="SendBox" InputScope="Chat"
                                 KeyDown="SendBoxKeyDown" DoubleTap="SendBoxDoubleTap" Tap="SendBoxTap" />
                    </Grid>
                    <Grid VerticalAlignment="Top" Height="50" Margin="-12,0">
                        <Grid.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#FFC0C0C0" Offset="0.0" />
                                <GradientStop Color="#00C0C0C0" Offset="1.0" />
                            </LinearGradientBrush>
                        </Grid.Background>
                    </Grid>
                    <ScrollViewer x:Name="ChatScroller" 
                                   Margin="0,0,0,64" Tap="ChatScrollerTap">
                        <ItemsControl x:Name="Chats" ItemTemplate="{StaticResource ChatMessageTemplate}" />
                    </ScrollViewer>
                </Grid>
            </controls:PivotItem>
            <controls:PivotItem x:Name="mediaTab" Header="Drops">
                <Grid Margin="-12,-30,-12,0">
                    <ScrollViewer>
                        <StackPanel>
                            <Grid Margin="0,0,0,-25" Height="100">
                                <Grid.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#FFF0F0F0" Offset="1" />
                                        <GradientStop Color="#FFA7A7A7" Offset="0.5" />
                                        <GradientStop Color="#FFA7A7A7" Offset="0" />
                                    </LinearGradientBrush>
                                </Grid.Background>
                                <Grid Height="75" VerticalAlignment="Top" Margin="12,0,12,0">
                                    <TextBox Margin="0,0,75,0" Name="SearchBar" />
                                    <Button HorizontalAlignment="Right" Width="75" Style="{StaticResource XTiltButtonStyle}"
                                        Padding="0" BorderBrush="Transparent" Click="SearchClick">
                                        <Button.Content>
                                            <Image Name="SearchButtonImage" Source="/Images/Icons/search.png" />
                                        </Button.Content>
                                    </Button>
                                </Grid>
                            </Grid>
                            <ItemsControl x:Name="MediaList" Margin="12,0,12,0">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Button BorderBrush="Transparent" Padding="-12" 
                                            Click="DropItemClick" DataContext="{Binding}" 
                                            Style="{StaticResource XTiltButtonStyle}"
                                            HorizontalAlignment="Left">
                                            <Grid>
                                                <Image DataContext="{Binding}" Source="{Binding Path=preview}"
                                                   Width="120" Height="90" HorizontalAlignment="Left"
                                                   VerticalAlignment="Top" ImageFailed="DropImageFailed" />
                                                <TextBlock DataContext="{Binding}" Text="{Binding Path=title}" 
                                                       Margin="125,0,0,0" TextWrapping="Wrap" Foreground="Black" 
                                                       FontSize="24" VerticalAlignment="Top" />
                                            </Grid>
                                        </Button>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <Button Width="200" Foreground="Black" BorderBrush="Black"
                                    Click="MoreDropsClick" Name="MoreDrops">Load More</Button>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </controls:PivotItem>
            <controls:PivotItem x:Name="peopleTab" Header="Ponies">
                <Grid Margin="0,-12,0,0">
                    <ScrollViewer>
                        <ItemsControl x:Name="Ponies">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button BorderBrush="Transparent" Padding="-12"
                                            Style="{StaticResource XTiltButtonStyle}"
                                            Click="UserListClick" Margin="0"
                                            HorizontalAlignment="Left">
                                        <Grid Height="50" Margin="0,0,0,10" HorizontalAlignment="Left">
                                            <Image Source="{Binding Path=avatar.Thumb}"
                                               Stretch="Fill"
                                               HorizontalAlignment="Left"
                                               Width="50" Height="50">
                                                <Image.Clip>
                                                    <RectangleGeometry RadiusX="5" RadiusY="5" Rect="0,0,50,50" />
                                                </Image.Clip>
                                            </Image>
                                            <TextBlock Text="{Binding Path=name}"
                                                   FontSize="34" Foreground="Black"
                                                   Margin="60,0,0,0"/>
                                        </Grid>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </controls:PivotItem>
        </controls:Pivot>
        <Button  HorizontalAlignment="Right" VerticalAlignment="Top" 
                 Padding="-10" BorderBrush="Transparent" Margin="0"
                 Style="{StaticResource XTiltButtonStyle}"
                 Click="AddOrRemoveCloudClick">
            <Button.Content>
                <Image Source="/Images/Icons/add.png" Width="40" />
            </Button.Content>
        </Button>
        <Popup Margin="10" Name="userpopup" Width="456" Height="776">
            <Border Margin="0,0,0,0">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Offset="0" Color="#FFF0F0F0" />
                        <GradientStop Offset="1" Color="#FFD9D9D0" />
                    </LinearGradientBrush>
                </Border.Background>
                <Grid Width="456" Height="776">
                    
                </Grid>
            </Border>
        </Popup>
    </Grid>

    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="False" IsMenuEnabled="False"/>
    </phone:PhoneApplicationPage.ApplicationBar>

</phone:PhoneApplicationPage>
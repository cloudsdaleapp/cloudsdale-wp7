<phone:PhoneApplicationPage 
    x:Class="Cloudsdale.Home"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="768"
    FontFamily="Segoe WP Bold"
    FontSize="28"
    Foreground="Black"
    SupportedOrientations="Portrait"  Orientation="Portrait"
    shell:SystemTray.IsVisible="True"
    shell:SystemTray.Opacity="0.01">

    <phone:PhoneApplicationPage.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="XamlResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </phone:PhoneApplicationPage.Resources>

    <Grid x:Name="LayoutRoot" Background="{StaticResource PortraitBackground}" Margin="0,0,0,0">
        <controls:Pivot Title="Cloudsdale" Name="pivotView" Margin="0,10,0,0">
            <controls:Pivot.HeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" FontFamily="Segoe WP Bold" FontSize="48"/>
                </DataTemplate>
            </controls:Pivot.HeaderTemplate>
            <controls:PivotItem Header="Home" Foreground="#FF2A81DB">
                <StackPanel x:Name="UserInfoPane">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{Binding Path=avatar.Normal}" Width="100" Height="100" 
                               HorizontalAlignment="Left" VerticalAlignment="Top">
                            <Image.Clip>
                                <RectangleGeometry RadiusX="10" RadiusY="10" Rect="0,0,100,100" />
                            </Image.Clip>
                        </Image>
                        <StackPanel>
                            <TextBlock Margin="10,0,0,0" Foreground="#FF202020"><Run Text="{Binding Path=name}" /></TextBlock>
                            <Button Content="Logout" Foreground="#FF202020" BorderBrush="#FF202020" Margin="0"
                                HorizontalAlignment="Left" VerticalAlignment="Top" FontSize="20" Click="LogoutClick" />
                        </StackPanel>
                    </StackPanel>
                    <TextBlock TextWrapping="Wrap" FontSize="34" 
                               Text="Welcome back to cloudsdale!" 
                               Margin="0,0,0,20" />
                    <TextBlock TextWrapping="Wrap" FontSize="18">
                        You have been a member since <Run Text="{Binding Path=member_since, StringFormat=dd/MM/yy}" />
                    </TextBlock>
                </StackPanel>
            </controls:PivotItem>
            <controls:PivotItem Header="Clouds">
                <Grid Margin="-12,-26,-12,0">
                    <ScrollViewer Margin="12,0,12,50">
                        <ItemsControl x:Name="CloudList" Margin="0,15,0,0" />
                    </ScrollViewer>
                    <Button VerticalAlignment="Bottom" BorderBrush="Transparent" Margin="0,0,0,2" 
                            Height="50" Padding="-25" Width="50"
                            Style="{StaticResource XTiltButtonStyle}">
                        <Image Source="/Images/Icons/refresh.png" Width="50" Height="50" />
                    </Button>
                </Grid>
            </controls:PivotItem>
            <controls:PivotItem Header="Explore" Foreground="Black" Visibility="Collapsed" IsEnabled="False">
                <Grid>
                    <Grid Margin="-12,-26,-12,-30" Height="110" VerticalAlignment="Top">
                        <Grid.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#FFF0F0F0" Offset="1" />
                                <GradientStop Color="#FFA7A7A7" Offset="0.6" />
                                <GradientStop Color="#FFA7A7A7" Offset="0" />
                            </LinearGradientBrush>
                        </Grid.Background>
                        <TextBox VerticalAlignment="Top" Margin="0,0,75,0" />
                        <Button VerticalAlignment="Top" HorizontalAlignment="Right" 
                                BorderBrush="Transparent" Margin="0,-9"
                                Style="{StaticResource XTiltButtonStyle}">
                            <Image Source="/Images/Icons/search.png" Width="50" />
                        </Button>
                    </Grid>
                    <ScrollViewer Margin="0,54,0,50">
                        <ItemsControl x:Name="searchResults">
                            <TextBlock Text="Loading..." />
                        </ItemsControl>
                    </ScrollViewer>
                    <Button VerticalAlignment="Bottom" BorderBrush="Transparent" Margin="0,0,0,2" 
                            Height="50" Padding="-25" Width="50" Click="ExploreRefreshClick"
                            Style="{StaticResource XTiltButtonStyle}">
                        <Image Source="/Images/Icons/refresh.png" Width="50" Height="50" />
                    </Button>
                    <Button VerticalAlignment="Bottom" HorizontalAlignment="Left" Content="Popular" 
                            Foreground="Black" BorderBrush="Black" Width="175" Margin="30,-8" 
                            Click="PopularClick" />
                    <Button VerticalAlignment="Bottom" HorizontalAlignment="Right" Content="Recent" 
                            Foreground="Black" BorderBrush="Black" Width="175" Margin="30,-8" 
                            Click="RecentClick" />
                </Grid>
            </controls:PivotItem>
            <controls:PivotItem Header="Settings">
                <Grid>
                    <ScrollViewer>
                        <StackPanel>
                            <Button Foreground="Black" BorderBrush="Black" Content="All Users Seen"
                                    Visibility="Collapsed"/>
                            <CheckBox x:Name="allowaltcodes" Content="Enable alt code entry" 
                                      Foreground="Black" Checked="AllowaltcodesChecked"
                                      Unchecked="AllowaltcodesUnchecked"/>
                            <CheckBox x:Name="recursivealtcodes" Content="Enter codes repeatedly"
                                      Foreground="Black" Margin="50,-20,0,0" Visibility="Collapsed"
                                      Checked="RecursivealtcodesChecked"
                                      Unchecked="RecursivealtcodesUnchecked"/>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </controls:PivotItem>
        </controls:Pivot>
    </Grid>

</phone:PhoneApplicationPage>
    